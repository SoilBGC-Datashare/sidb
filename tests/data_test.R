library(testthat)
library(sidb)

path_to_data <- "~/sidb/data/" # use absolute path for the directory where SIDb data are stored
test_dir("testthat") # this should be the path to the directory containing the test file "test_dataStructure.R"

